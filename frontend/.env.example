# Local dev: leave unset — Vite proxies /api and /health to the backend (vite.config.js).
# Vercel production: set VITE_API_BASE_URL in the Vercel project (Settings → Environment Variables),
# e.g. https://nyayamitra-api.onrender.com (no trailing slash).
# VITE_API_BASE_URL=
